# 小说分析与创作知识服务开发约定

## 工作依据与判断

- 协助部署 embedding 时，先阅读 [部署指南的 AI 操作指引](docs/部署指南.md#ai-协助准备-embedding)。由 AI 检查本机资源、选择并验证候选参数，程序只执行配置校验、启动及验证；复用有效配置，不擅自更换模型契约或覆盖损坏配置。

- 执行环境相关命令前，读取 [本地环境说明](docs/本地环境.local.md)（若存在），并结合当前环境核实本次所需的工具和服务入口。文件缺失时依据 [部署指南](docs/部署指南.md) 和 [环境说明模板](docs/本地环境.example.md) 检查本次需要的环境项，不要求先创建记录文件或填写全部字段。历史验证记录不代表当前环境已经验证通过；本地说明是可选的实机记录，不是程序配置，不覆盖项目规格和依赖声明，不记录密码或密钥。
- 开始设计或修改前，阅读 [需求与设计基线](docs/小说分析与创作知识服务_需求与设计基线.md) 的相关章节，以及当前 Spec、相关计划、代码、配置和测试，不仅依据历史会话结论行动；[分析指南](docs/小说作品深度分析与写法标注指南.md)和[写作原则](docs/中文小说正文写作原则.md)按需读取。
- 需求基线和已确认 Spec 定义目标行为，代码与配置描述实际实现，测试与运行验证提供行为证据。存在不一致时，明确指出目标、现状和缺口，不默认代码或日期较新的文档就是正确要求。
- 用户最新明确决定优先于文档中的旧约定，相关文档应随本次变更同步更新。候选方案和未决问题不得当作已确认要求。
- 后端使用 Python。其他技术和目录结构随实际需求确定，不提前建立完整框架或批量编写详细设计。

## 规格驱动开发

- 新增功能或明显改变行为前，先在 `specs/` 编写或修订简明 Spec，说明目标、范围、关键行为和验收条件。小型修复沿用现有 Spec，不机械新增文档。
- 默认围绕当前功能逐步推进。取得明确的开发授权后，在授权范围内连续完成必要的规格细化、实现和验证，不为普通实现步骤反复确认；涉及未确认的产品语义或范围扩大时再与用户对齐。
- Spec 维护当前有效的要求，实施计划不能扩展或覆盖 Spec。实现不符合要求时修复实现，不通过修改规格或削弱验收标准掩盖问题。

## 实现原则

- 以完成已确认的业务行为和验收标准为目标。新增模块、抽象、依赖和配置必须对应实际需求或现实风险，不预建空分层、重复包装或没有调用方的扩展机制。
- 简化实现不能省略必要的校验、事务、并发控制、数据库约束和失败处理。审查意见按影响区分“本次阻塞”“需要确认”“范围外建议”，不把所有可改进项都纳入本次开发。
- 文学判断由外部 AI 完成，服务负责原文、分析资产、检索和进度等稳定数据能力。HTTP、MCP、CLI 共用业务规则。
- 原文保真、稳定定位、作品范围隔离、写入幂等和进度一致性属于必要行为，不能以简化实现为由省略；具体契约在相关 Spec 中落实。

## 代码与注释

- 代码标识符、API 字段、配置键和错误码使用英文；公共接口和核心数据结构提供明确的类型标注，注释与 docstring 使用简体中文。
- 对需要解释的类、函数、业务规则、外部接口适配、事务、并发、异常处理和非直观算法，必须补充必要的 docstring 或注释，说明用途、原因、前置条件、输入输出、失败行为及副作用，使不了解当前会话的人能够安全修改代码。
- SQL 和迁移脚本应说明表的职责、关键字段语义，以及约束、索引和幂等设计的依据。注释不逐句复述代码，不添加模板化空话；行为变化时同步更新或删除失效说明。
- 注释、接口说明和项目文档采用中性、陈述式表达，写清对象、前提和行为，不依赖聊天上下文或隐含背景。

## 文件与数据

- 需求与方法文档放在 `docs/`，小说素材放在 `data/`，功能 Spec 放在 `specs/` 并按需创建。`AGENTS.md` 保留在项目根目录，只维护长期开发规则，不记录任务进度和历史总结；新增文件沿用已有组织方式，不为每次会话创建新的文档体系。
- 修改行为、接口、数据结构或配置时，同步维护受影响的当前 Spec、接口说明、配置示例、客户端适配和测试。历史记录保留当时语义；已有工具生成的文件通过生成流程更新，不手工改写。
- 小说原始素材不得被格式整理、测试或临时脚本覆盖；测试使用独立的小型样例。临时产物与需要保留的资料明确区分。
- 不将密钥、完整小说正文或完整请求体写入日志；协议响应与运行日志分离，应用日志写入 stderr。

## 验证与交付

- 验证可观察行为、数据完整性和实际回归风险，不用源码字符串、框架自带行为或无意义的 mock 断言代替行为测试。SDD、TDD、测试数量和覆盖率不能成为机械增加测试的理由。
- 验证范围与变更影响相称。没有有价值的自动化测试时，采用适当的静态检查、真实接口或命令验证、人工检查，并记录结果；新变更未影响已验证行为时，不重复执行无关检查。
- 数据库约束、事务和检索行为在实际选定的数据库及扩展上验证。未执行或受阻的检查必须说明，不能视为通过。
- 当前范围实现、必要验证通过、相关文档同步后才能标记完成。交付时简述结果、验证和未完成项，不以空函数、固定假结果或 TODO 代替功能。
- Git 提交、推送、发布和部署遵循用户的明确授权，不因完成开发而自动执行。
