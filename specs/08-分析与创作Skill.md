# 08 分析与创作 Skill

状态：两份 Skill 已实现，格式、独立资源与单份资料同步验证通过；实际 AI 宿主联调与文学效果评估后置。不新增服务接口、检索索引或数据库迁移。

依据：需求基线第 24、32.5、35 章，两份方法文档，Spec 02–07 的工具契约、Spec 09 的关键词检索增量，以及已确认的精简与独立使用要求。

## 1. 范围与交付

在 `.agents/skills/` 维护 novel-analysis 与 chinese-novel-writing 两份 Skill。每个完整文件夹可独立交付，不依赖开发仓库、其他 Skill 或历史会话；按任务需要使用已连接的 NovelLens MCP。

Skill 主要补充任务标准和工具约束，依靠 LLM 自身的阅读、分析和创作能力，不把完整分析指南、44 项维度、专项教程和大量示例作为运行资料。完整分析指南保留在 docs/ 供维护参考；分析 Skill 自身须写清必要的证据、标注质量与工作边界。

中文写作原则只随 chinese-novel-writing 携带一份，保留六项核心原则、创作取向和定向修订方法。维护源仍为 docs/中文小说正文写作原则.md；同步脚本只负责该单份副本，运行 Skill 无需脚本或 Python。两个入口及各自 MCP 流程人工维护，不自动复制其他文档。

本期不安装全局 Skill、不配置实际 AI 宿主、不修改服务代码或运行依赖、不操作共享数据库、不执行 Git 提交推送。

## 2. novel-analysis

- 面向指定作品范围的深读、标注和资产修订，也支持接续已有分析任务。仅查看和讨论时只读；明确要求保存时在目标范围内连续处理。
- 说明具体文本选择、组织关系、必要条件和适用边界，用真实原文支持结论；区分事实、解释和未知，不以剧情概括或空泛评价代替分析。
- 不固定审美偏好，不替参考作者改稿，不机械填维度，不跳过普通片段，不把局部证据扩大为全书规律。
- 区分 Annotation、Tag、Entity、Relation、StyleGuide 和任务进度；给出必要的选择规则，按需读取包内 MCP 参数与恢复流程。
- 沿用稳定定位、作品隔离、真实分页、版本校验、固定请求键和原子 checkpoint；新标签或实体需先取得可引用 ID。只对已按任务处理并成功提交的范围推进进度。
- 新会话从当前任务、Coverage 和 recovery 恢复；缺失的未提交请求不能假称可恢复。结果不明、完成和重开行为遵循既有契约。

## 3. chinese-novel-writing

- 统一处理中文小说构思、正文创作、续写与修订。普通写作采用用户任务和前文，无需 MCP，也不加载 MCP 调用说明。
- 读取包内写作原则，保留事实与人物依据、因果可信度、信息与声音归属、参考迁移和表达自由，以及想象力、情绪、期待和愿望兑现的创作取向。
- 用户要求参考 NovelLens，或当前创作确需且允许使用该库时，按需读取包内 MCP 查询流程。参考库查询不再拥有独立 Skill 入口。
- 使用风格导航、标签/实体字面查询、标注过滤、关系查询和原文读取；Spec 09 增加原文及写法说明关键词检索，按实际工具发现与启用状态使用，不虚构向量或统一 Search 工具。
- 先核对候选与完整原文，再判断参考写法在新作中的适用条件。用户的新作事实优先，不照搬参考作品的人物经历与关系基础。
- 参考库默认只读，不因写作修改资产或推进分析进度。必要证据不可用时说明缺口；用户允许不依赖参考时可继续，必须使用指定参考时则保留阻塞。

## 4. 验收与维护

1. 最终只有两份可发现的 Skill，职责明确；移除已合并入口、分析指南副本和分析包中的写作原则，以及对应生成逻辑与失效引用。
2. 每份 Skill 单独复制到仓库外后，全部运行资料在本包内可达；不存在对 docs/、specs/、其他 Skill 或固定本机路径的依赖。
3. 写作原则的唯一包内副本与维护源一致；同步可重复执行，检查模式能发现源文档或副本变化。分析指南不再自动生成 Skill 文件。
4. 保留必要的 MCP 调用顺序、参数、分页、版本与失败恢复约束。未改变的请求模板沿用既有真实 MCP 验证；变更了接口语义时才重跑受影响的调用验证。
5. 同步 README 和需求基线。格式和资源检查不能冒称已证明 AI 自动触发、文学判断或实际宿主效果；这些联调与效果评估继续后置。

Spec 10 的全文增量为两份 Skill 增加 `semantic_index_get` 与 `source_semantic_search` 的调用规则：先核对作品和覆盖，再按实际 SourceRange 回读证据；候选分数与索引覆盖不代表文学判断或分析进度。分析包另外说明用户要求构建时的 create / build 请求键与接续流程，写作包保持默认只读。当前只支持 fulltext，不发布标注原文层的占位调用。该适配不改变分析标准、写作原则或两包独立性；真实协议验证见 Spec 10 的全文增量验收，实际 AI 宿主与文学效果仍待验证。

## 5. 验证记录

2026-09-20：

- 两份 Skill 通过 quick_validate.py；分别复制到仓库外单独目录后，入口可达全部 5 个文件，3 处文件链接均在各自包内解析，无仓库或跨 Skill 依赖。分析包 2 个文件，中文写作包 3 个文件。
- 单份写作原则同步验证覆盖缺失副本、重复执行、源文档变更、副本误改与修复；只生成写作包的一个文件，不生成分析资料。同步脚本 Ruff、格式与严格 mypy 通过，git diff --check 通过。
- 7 个 JSON MCP 请求模板与调整前逐项一致。本次只改变入口组织、资料范围与辅助说明，未重新访问数据库或重跑服务测试；独立验证的临时目录已清理。
- README 与需求基线已同步。已合并的参考库写作入口、分析指南副本和重复写作原则已清理；完整分析指南仍保留作维护参考。

既有服务证据：上述 7 个模板在独立 PostgreSQL 18.6 测试库通过官方 MCP 客户端完成 26 次调用，覆盖任务创建、原文读取、checkpoint、完成、历史重放和只读查询前后数据不变；临时服务和数据库已清理。该证据仅覆盖工具链，不证明 Skill 的文学效果。实际 AI 宿主触发、多轮行为和创作质量尚未验证。
